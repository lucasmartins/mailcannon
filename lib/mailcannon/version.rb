# Keeps the versioning clean and simple.
module MailCannon
  module Version
    MAJOR = 0
    MINOR = 3
    PATCH = 9
    ALPHA = nil # ex: '.pre.1'
    STRING = "#{MAJOR}.#{MINOR}.#{PATCH}#{ALPHA}"
  end
end
